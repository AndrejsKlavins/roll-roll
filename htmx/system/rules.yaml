# RPG system definition. Loaded once at server startup — restart after editing.
#
# Sections (or single number fields) marked "base: true" get base values. While a
# character is in creation they are edited freely and nothing is logged. "Finish
# character" locks them in as base values; changes during play are shown against
# the base, e.g. "3 (base 4)".
#
# Field types:
#   number  — stepper with − / + buttons (min, max, default)
#   track   — row of clickable pips from 0 to max (default)
#   text    — free text box (lines = height)
#   level   — the character's level with a "Level up" button (one per sheet; needs "training:")
#   derived — calculated number (formula); can be placed in any section. Finished characters
#             can apply play changes with ✎. "pool: true" makes the formula a maximum shown
#             as circles (filled = left, empty = spent), e.g. Health.
#
# Any field can have a look:
#   icon    — file in system/icons/ (SVG recommended; drawn in white on the colour)
#             or a short text/emoji, e.g. icon: "⚔"
#   color   — hex colour in quotes, e.g. color: "#99342c"
#
# Formulas (derived values and rolls) can use:
#   field ids and earlier derived ids, numbers, + - * / ( ),
#   floor() ceil() round() min() max() abs()
#   dice (rolls only): 2d6, d20, (body + athletics)d6
# Field ids must not look like dice (d6, d20).
#
# Traits: bundled adjustments a player can pick instead of hand-editing steppers (up to 8,
# both during creation and, if the GM allows it, later — adding/dropping one on a finished
# character is a base-value change, logged like any other). "cost" runs -2..2 (negative =
# buff, positive = flaw, 0 = a mix); it is advisory only — the running total vs. "power_level"
# is shown but nothing is blocked. "power_level" is the GM's default target; adjustable at
# the table from the GM page without editing this file.
#
# Each modifier targets either an untrained base field (ability) with "delta" (nudges it,
# e.g. -1..2), or a trained field (skill) with "skill_points" (pre-loads that many points —
# same currency as training — which may push its rank up once enough are on it).
#
# Every trait belongs to one of "categories" (each gets its own "add" picker on the
# sheet). "max" caps how many from that category can be picked at once (omit for no
# cap) — once hit, that category's "add" picker is hidden, e.g. Origin: max 1. Traits
# are offered in rules.yaml list order by default (e.g. Basic follows the abilities'
# order on the sheet) — add "sort: alpha" to a category to offer its traits A-Z instead.
#
# "tags" (optional) make traits mutually exclusive: picking one hides every other trait
# sharing any of its tags, e.g. Strong/Very strong/Weak/Very weak all tagged "base_strength"
# so only one Strength-affecting basic trait can ever be picked at once.

name: Sample System

# Named word scales. A number field (or section) with "scale: <name>" shows the word and
# one dot per point in its colour instead of the number.
scales:
  rating:
    0: abysmal
    1: horrible
    2: low
    3: average
    4: high
    5: excellent
    6: epic
  skill:
    0: untrained
    1: novice
    2: experienced
    3: advanced
    4: expert
    5: master

# Skill point training. Finishing a character and each level up grant the value of
# points_stat. Rank n costs rank_costs[n-1] more points: 4, 5, 6, 7, 8 → 4, 9, 15, 22, 30 total.
training:
  points_stat: skill_points
  rank_costs: [4, 5, 6, 7, 8]

# Challenges (public-screen board, /table): a difficulty ladder the GM picks the challenge's one
# difficulty from (then hand-adjusts with the circumstance stepper), the framing ladder, and the
# named approaches offered to the rolling player. Tweak "value" freely — nothing else depends on it.
challenges:
  difficulties:
    - { id: easy,        label: Easy,        value: 4 }
    - { id: challenging, label: Challenging, value: 7 }
    - { id: hard,        label: Hard,        value: 10 }
    - { id: very_hard,   label: Very hard,   value: 13 }
    - { id: epic,        label: Epic,        value: 16 }
  # The framing roll sets up the resolution roll. Its margin (framing result − difficulty) picks
  # a rung: "from" is the lowest margin that rung covers, and it runs up to the next rung's
  # "from". The bottom rung also catches everything below it, so the bands need not be even.
  #   bonus    added straight onto the resolution roll's sum; a plus helps, a minus hurts
  #   degrees  a boon (+) or a complication (−), added to the resolution's own degrees
  # Both are arithmetic on purpose: the two rolls land together and the rung is worked out live,
  # so exertion on the framing moves the resolution's bonus after the dice are already down.
  framing:
    ladder:
      #  from   the band it covers
      - { from: -9, bonus: -6, degrees: -1, label: Critical disadvantage — a complication }  # −9 or worse
      - { from: -8, bonus: -3, degrees: -1, label: A big disadvantage — a complication }     # −8 … −6
      - { from: -5, bonus: -1, degrees: -1, label: A disadvantage — a complication }         # −5 … −3
      - { from: -2, bonus: -1,              label: Slightly off }                            # −2, −1
      - { from: 0,                          label: The situation is even }                   # 0 … 2
      - { from: 3,  bonus: 1,               label: An advantage }                            # 3 … 5
      - { from: 6,  bonus: 3,               label: A big advantage }                         # 6 … 8
      - { from: 9,  bonus: 6,               label: Critical advantage }                      # 9 or better
  # Each approach rolls its own d6 (the approach die) alongside the challenge dice. "when" says
  # whether that die's effect applies: always, only if the framing or the resolution roll was
  # short of its target as the dice landed (later exertion doesn't undo that), or by the player's
  # choice (an Activate button).
  # "effects" says what each face of that approach's die does, and therefore what the player is
  # asked to tap after pressing Activate (nothing, for effects that need no target).
  # Faces with no effect (or kind: none) offer no Activate button. Any cost for an effect is
  # paid at the table — the app never deducts one.
  # "description" is free text shown beside the pick button — say what the approach is for.
  approaches:
    - id: unbreakable
      label: Unbreakable
      when: failure
      description: The effect triggers only if you are failing.
      effects:
        - { face: 1, kind: none }
        - { face: 2, kind: declare, label: Unshakable — ignore any negative effects of the failure }
        - { face: 3, kind: raise_face, dice: 2, label: Recall your source of strength — raise 2 dice one face }
        - { face: 4, kind: raise_face, dice: 2, label: Recall your source of strength — raise 2 dice one face }
        - { face: 5, kind: set_face, dice: 2, to_face: 3, label: Squash weakness — set 2 dice to their 3rd face }
        - { face: 6, kind: set_face, dice: 2, to_face: 3, label: Squash weakness — set 2 dice to their 3rd face }
    - id: exquisite
      label: Exquisite
      when: choice
      description: You choose whether to activate this dice.
      effects:
        - { face: 1, kind: none }
        # Labels with a comma must be quoted: an unquoted one would end at the comma in { } flow.
        - { face: 2, kind: lower_raise,    label: "Tweak — lower one die a face, raise another a face" }
        - { face: 3, kind: lower_raise,    label: "Tweak — lower one die a face, raise another a face" }
        # Setup's "Improved" (set any die to its max on a later roll with the lowered die's ability)
        # is tracked at the table; the app only enforces the lowering.
        - { face: 4, kind: lower_face,     label: "Setup — lower a die one face; your next roll with that ability is Improved (set any die to max)" }
        - { face: 5, kind: lower_face,     label: "Setup — lower a die one face; your next roll with that ability is Improved (set any die to max)" }
        - { face: 6, kind: max_face,       label: "Perfect choice — set one die to its maximum" }
    - id: limitless
      label: Limitless
      when: always
      description: Effect always happens.
      effects:
        - { face: 1, kind: discard }
        - { face: 2, kind: none }
        - { face: 3, kind: reroll,     label: Emotional outburst - Reroll one die and get a hindrance (-1 next roll) }
        - { face: 4, kind: extra_dice, dice: 1, label: Wreck it - Roll one extra die on a roll of your choice and break something }
        - { face: 5, kind: extra_dice, dice: 1, label: Push too far - Roll one extra die on a roll of your choice and lose a resource }
        - { face: 6, kind: extra_dice, dice: 2, label: Lose your head - Roll two extra dice on a roll of your choice and get a light injury }
  # Pool stats a player can burn during a challenge: 1 point → 1 exertion, spent as +1 on the
  # open roll or to reroll one of its dice. Must be pools (pool: true).
  exertion_sources: [stamina, willpower]
  # What each rolled die face is called, worst (red) to best (green). A rank shifts every face,
  # so results outside this list use the nearest end (e.g. a 7 reads as "amazing").
  faces:
    - { value: 1, label: horrible, color: "#c0392b" }
    - { value: 2, label: poor,     color: "#d1683a" }
    - { value: 3, label: ok,       color: "#d6a648" }
    - { value: 4, label: good,     color: "#b3bf4f" }
    - { value: 5, label: great,    color: "#79b455" }
    - { value: 6, label: amazing,  color: "#45a862" }

traits:
  power_level: 0
  categories:
    - { id: basic,      label: Basic }
    - { id: background, label: Background }
    - { id: origin,     label: Origin, max: 1, sort: alpha }
    - { id: physique,   label: Physique }
  list:
    - { id: raised_in_wilds,     label: Raised in Wilds,     category: origin, cost: 0,
        description: "+1 Perception, +1 Agility, -2 Knowledge, -1 Manipulation, +4 skill points to Outdoorsman.",
        modifiers: [{ field: perception, delta: 1 }, { field: agility, delta: 1 }, { field: knowledge, delta: -2 }, { field: manipulation, skill_points: -1 }, { field: outdoorsman, skill_points: 4 }] }
    - { id: nobility,            label: Nobility,            category: origin, cost: -1,
        description: "+1 Intuition, +1 Knowledge, +4 skill points to Melee combat, +4 skill points to Manipulation, -1 Resolve, -1 Perception, -1 Endurance.",
        modifiers: [{ field: intuition, delta: 1 }, { field: knowledge, delta: 1 }, { field: melee_combat, skill_points: 4 }, { field: manipulation, skill_points: 4 }, { field: resolve, delta: -1 }, { field: perception, delta: -1 }, { field: endurance, delta: -1 }] }
    - { id: street_urchin,       label: Street urchin,       category: origin, cost: -1,
        description: "+1 Perception, +1 Agility, -2 Knowledge, +4 skill points to Thieving, +4 skill points to Stealth, +1 Evasion.",
        modifiers: [{ field: perception, delta: 1 }, { field: agility, delta: 1 }, { field: knowledge, delta: -2 }, { field: thieving, skill_points: 4 }, { field: stealth, skill_points: 4 }, { field: evasion, delta: 1 }] }
    - { id: raised_by_monks,     label: Raised by monks,     category: origin, cost: 0,
        description: "+1 Resolve, +1 Endurance, -1 Intuition.",
        modifiers: [{ field: resolve, delta: 1 }, { field: endurance, delta: 1 }, { field: intuition, delta: -1 }] }
    - { id: brawler,             label: Brawler,             category: origin, cost: 0,
        description: "+1 Strength, -1 Knowledge, -1 Intuition, +4 skill points to Melee combat, +1 Health.",
        modifiers: [{ field: strength, delta: 1 }, { field: knowledge, delta: -1 }, { field: intuition, delta: -1 }, { field: melee_combat, skill_points: 4 }, { field: health, delta: 1 }] }
    - { id: gang_leader,         label: Gang leader,         category: origin, cost: 0,
        description: "+1 Intuition, -1 Endurance, -1 Strength, +4 skill points to Manipulation, +4 skill points to Melee combat.",
        modifiers: [{ field: intuition, delta: 1 }, { field: endurance, delta: -1 }, { field: strength, delta: -1 }, { field: manipulation, skill_points: 4 }, { field: melee_combat, skill_points: 4 }] }
    - { id: raised_by_performers, label: Raised by performers, category: origin, cost: 0,
        description: "+1 Agility, -2 Knowledge, +4 skill points to Acrobatics, +4 skill points to Performance, +1 Evasion.",
        modifiers: [{ field: agility, delta: 1 }, { field: knowledge, delta: -2 }, { field: acrobatics, skill_points: 4 }, { field: performance, skill_points: 4 }, { field: evasion, delta: 1 }] }
    - { id: spoiled_child,       label: Spoiled Child,       category: origin, cost: 1,
        description: "-1 Stamina, -1 Willpower, -1 Health, -1 Mind.",
        modifiers: [{ field: stamina, delta: -1 }, { field: willpower, delta: -1 }, { field: health, delta: -1 }, { field: mind, delta: -1 }] }
    - { id: bookworm,            label: Bookworm,            category: origin, cost: 0,
        description: "+2 Knowledge, -1 Strength, -1 Agility, -1 Endurance, +4 skill points to Medicine, +4 skill points to Theory.",
        modifiers: [{ field: knowledge, delta: 2 }, { field: strength, delta: -1 }, { field: agility, delta: -1 }, { field: endurance, delta: -1 }, { field: medicine, skill_points: 4 }, { field: theory, skill_points: 4 }] }
    - { id: abusive_parents,     label: Abusive parents,     category: origin, cost: 0,
        description: "+1 Resolve, +1 Endurance, -2 Mind, -2 Health.",
        modifiers: [{ field: resolve, delta: 1 }, { field: endurance, delta: 1 }, { field: mind, delta: -2 }, { field: health, delta: -2 }] }
    # Stealth (Big/Small) and the performance/manipulation roll bonus (Beautiful/Ugly) are
    # situational, not a stat we can automate yet (no roll system, no per-roll modifiers) — kept
    # in the description only, for whoever resolves the roll to apply by hand.
    - { id: big,       label: Big,       category: physique, cost: -1, description: "+1 Strength, +1 Endurance, -1 Evasion, -1 Speed, -2 Stealth.", modifiers: [{ field: strength, delta: 1 }, { field: endurance, delta: 1 }, { field: evasion, delta: -1 }, { field: speed, delta: -1 }] }
    - { id: thin,      label: Thin,      category: physique, cost: 0,  description: "+1 Agility, +1 Evasion, -1 Strength, -1 Physical resistance.", modifiers: [{ field: agility, delta: 1 }, { field: evasion, delta: 1 }, { field: strength, delta: -1 }, { field: physical_resistance, delta: -1 }] }
    - { id: small,     label: Small,     category: physique, cost: 1,  description: "-1 Strength, -1 Endurance, +1 Evasion, +1 Speed, +2 Stealth.", modifiers: [{ field: strength, delta: -1 }, { field: endurance, delta: -1 }, { field: evasion, delta: 1 }, { field: speed, delta: 1 }] }
    - { id: fat,       label: Fat,       category: physique, cost: 0,  description: "-1 Agility, -1 Evasion, +1 Strength, +1 Physical resistance.", modifiers: [{ field: agility, delta: -1 }, { field: evasion, delta: -1 }, { field: strength, delta: 1 }, { field: physical_resistance, delta: 1 }] }
    - { id: stocky,    label: Stocky,    category: physique, cost: -1, description: "+1 Endurance, +1 Physical resistance.",                        modifiers: [{ field: endurance, delta: 1 }, { field: physical_resistance, delta: 1 }] }
    - { id: delicate,  label: Delicate,  category: physique, cost: 1,  description: "-1 Endurance, -1 Physical resistance.",                        modifiers: [{ field: endurance, delta: -1 }, { field: physical_resistance, delta: -1 }] }
    - { id: beautiful, label: Beautiful, category: physique, cost: 0,  description: "+2 to relevant performance and manipulation rolls, -1 Resolve.", modifiers: [{ field: resolve, delta: -1 }] }
    - { id: ugly,      label: Ugly,      category: physique, cost: 0,  description: "-2 to relevant performance and manipulation rolls, +1 Resolve.", modifiers: [{ field: resolve, delta: 1 }] }
    - { id: strong,               label: Strong,               category: basic, tags: [base_strength],   cost: -1, description: "+1 Strength",   modifiers: [{ field: strength, delta: 1 }] }
    - { id: very_strong,          label: Very strong,          category: basic, tags: [base_strength],   cost: -2, description: "+2 Strength",   modifiers: [{ field: strength, delta: 2 }] }
    - { id: weak,                 label: Weak,                 category: basic, tags: [base_strength],   cost: 1,  description: "-1 Strength",   modifiers: [{ field: strength, delta: -1 }] }
    - { id: very_weak,            label: Very weak,            category: basic, tags: [base_strength],   cost: 2,  description: "-2 Strength",   modifiers: [{ field: strength, delta: -2 }] }
    - { id: agile,                label: Agile,                category: basic, tags: [base_agility],    cost: -1, description: "+1 Agility",    modifiers: [{ field: agility, delta: 1 }] }
    - { id: very_agile,           label: Very agile,           category: basic, tags: [base_agility],    cost: -2, description: "+2 Agility",    modifiers: [{ field: agility, delta: 2 }] }
    - { id: clumsy,               label: Clumsy,               category: basic, tags: [base_agility],    cost: 1,  description: "-1 Agility",    modifiers: [{ field: agility, delta: -1 }] }
    - { id: very_clumsy,          label: Very clumsy,          category: basic, tags: [base_agility],    cost: 2,  description: "-2 Agility",    modifiers: [{ field: agility, delta: -2 }] }
    - { id: perceptive,           label: Perceptive,           category: basic, tags: [base_perception], cost: -1, description: "+1 Perception", modifiers: [{ field: perception, delta: 1 }] }
    - { id: very_perceptive,      label: Very perceptive,      category: basic, tags: [base_perception], cost: -2, description: "+2 Perception", modifiers: [{ field: perception, delta: 2 }] }
    - { id: imperceptive,         label: Imperceptive,         category: basic, tags: [base_perception], cost: 1,  description: "-1 Perception", modifiers: [{ field: perception, delta: -1 }] }
    - { id: very_imperceptive,    label: Very imperceptive,    category: basic, tags: [base_perception], cost: 2,  description: "-2 Perception", modifiers: [{ field: perception, delta: -2 }] }
    - { id: knowledgeable,        label: Knowledgable,         category: basic, tags: [base_knowledge],  cost: -1, description: "+1 Knowledge",  modifiers: [{ field: knowledge, delta: 1 }] }
    - { id: very_knowledgeable,   label: Very knowledgable,    category: basic, tags: [base_knowledge],  cost: -2, description: "+2 Knowledge",  modifiers: [{ field: knowledge, delta: 2 }] }
    - { id: unschooled,           label: Unschooled,           category: basic, tags: [base_knowledge],  cost: 1,  description: "-1 Knowledge",  modifiers: [{ field: knowledge, delta: -1 }] }
    - { id: very_unschooled,      label: Very unschooled,      category: basic, tags: [base_knowledge],  cost: 2,  description: "-2 Knowledge",  modifiers: [{ field: knowledge, delta: -2 }] }
    - { id: intuitive,            label: Intuitive,            category: basic, tags: [base_intuition],  cost: -1, description: "+1 Intuition",  modifiers: [{ field: intuition, delta: 1 }] }
    - { id: very_intuitive,       label: Very intuitive,       category: basic, tags: [base_intuition],  cost: -2, description: "+2 Intuition",  modifiers: [{ field: intuition, delta: 2 }] }
    - { id: unintuitive,          label: Unintuitive,          category: basic, tags: [base_intuition],  cost: 1,  description: "-1 Intuition",  modifiers: [{ field: intuition, delta: -1 }] }
    - { id: very_unintuitive,     label: Very unintuitive,     category: basic, tags: [base_intuition],  cost: 2,  description: "-2 Intuition",  modifiers: [{ field: intuition, delta: -2 }] }
    # "starts with gear/money" is kept in the description only: Money and equipment are on the
    # sheet now, but nothing fills them in automatically from a background.
    - { id: military, label: Military, category: background, cost: -1,
        description: "+4 skill points to Melee combat, +4 skill points to Ranged combat. Starts with basic combat gear.",
        modifiers: [{ field: melee_combat, skill_points: 4 }, { field: ranged_combat, skill_points: 4 }] }
    - { id: merchant, label: Merchant, category: background, cost: -1,
        description: "+4 skill points to Manipulation, +4 skill points to Performance. Starts the game with 50% more money than others.",
        modifiers: [{ field: manipulation, skill_points: 4 }, { field: performance, skill_points: 4 }] }
    - { id: thief, label: Thief, category: background, cost: -1,
        description: "+4 skill points to Thieving, +4 skill points to Stealth. Starts with basic thieving gear.",
        modifiers: [{ field: thieving, skill_points: 4 }, { field: stealth, skill_points: 4 }] }
    - { id: healer, label: Healer, category: background, cost: -1,
        description: "+4 skill points to Medicine, +4 skill points to Theory. Starts with basic medic gear.",
        modifiers: [{ field: medicine, skill_points: 4 }, { field: theory, skill_points: 4 }] }
    - { id: academy, label: Academy, category: background, cost: -1,
        description: "+4 skill points to Theory, +4 skill points to Stealth. Starts with one knowledge book.",
        modifiers: [{ field: theory, skill_points: 4 }, { field: stealth, skill_points: 4 }] }
    - { id: hunter, label: Hunter, category: background, cost: -1,
        description: "+4 skill points to Ranged combat, +4 skill points to Outdoorsman. Starts with basic hunter gear.",
        modifiers: [{ field: ranged_combat, skill_points: 4 }, { field: outdoorsman, skill_points: 4 }] }

sections:
  - label: Bio
    # compact: on the player's own sheet, one summary row with an ✎ button to edit.
    compact: true
    fields:
      # "full_name" (not "name") so it doesn't get confused with the login/character
      # identity name used for join and the top bar — that one is set at character creation.
      - { id: full_name, label: Name,       type: text, lines: 1 }
      - { id: age,        label: Age,        type: text, lines: 1 }
      - { id: profession, label: Profession, type: text, lines: 1 }
      - { id: race,       label: Race,       type: text, lines: 1 }
      # Level counter + "Level up" button (each level up grants Max skill points).
      - { id: level,      label: Level,      type: level }

  # No min/max: abilities scale indefinitely either way. The "rating" scale's own top and
  # bottom step (excellent/epic... abysmal) is used for anything beyond its defined range.
  - label: Core Abilities
    base: true
    scale: rating
    fields:
      - { id: strength,   label: Strength,   type: number, default: 3, icon: strength.svg,   color: "#c74446" }
      - { id: agility,    label: Agility,    type: number, default: 3, icon: agility.svg,    color: "#a7a758" }
      - { id: perception, label: Perception, type: number, default: 3, icon: perception.svg, color: "#ebac4d" }
      - { id: knowledge,  label: Knowledge,  type: number, default: 3, icon: knowledge.svg,  color: "#7dadc8" }
      - { id: intuition,  label: Intuition,  type: number, default: 3, icon: intuition.svg,  color: "#af6d77" }

  - label: Supporting Abilities
    base: true
    scale: rating
    fields:
      - { id: endurance, label: Endurance, type: number, default: 3, icon: endurance.svg, color: "#975638" }
      - { id: resolve,   label: Resolve,   type: number, default: 3, icon: resolve.svg,   color: "#556a97" }

  # Calculated stats (type: derived) — read-only numbers worked out from the formula.
  - label: Body
    fields:
      - { id: health, label: Health, type: derived, pool: true, formula: "endurance + floor(strength / 2)", icon: health.svg, color: "#c74446" }
      - { id: mind,   label: Mind,   type: derived, pool: true, formula: "resolve + floor(intuition / 2)",  icon: mind.svg,   color: "#5c81a3" }

  - label: Reserves
    fields:
      - { id: stamina,   label: Stamina,   type: derived, pool: true, formula: "endurance", icon: stamina.svg,   color: "#d0763e" }
      - { id: willpower, label: Willpower, type: derived, pool: true, formula: "resolve",   icon: willpower.svg, color: "#7dadc8" }

  - label: Defense
    fields:
      - { id: evasion,             label: Evasion,             type: derived, formula: "1 + agility + perception", icon: evasion.svg,             color: "#a7a758" }
      - { id: physical_resistance, label: Physical resistance, type: derived, formula: "2 + endurance",            icon: physical_resistance.svg, color: "#a6a6a6" }
      - { id: mental_resistance,   label: Mental resistance,   type: derived, formula: "2 + resolve",              icon: mental_resistance.svg,   color: "#b0d6d9" }

  - label: Misc
    fields:
      - { id: speed, label: Speed, type: derived, formula: "floor((agility + perception) / 2)", icon: speed.svg, color: "#f2d08d" }
      # Skill points gained per level (and on finishing the character). "base: true" = only base
      # ability values count, so temporary changes to Knowledge etc. don't affect it.
      - { id: skill_points, label: Max skill points, type: derived, base: true, formula: "3 + knowledge + intuition + resolve", icon: skill_points.svg, color: "#f2d08d" }

  - label: Skills
    trained: true   # rank comes from skill points assigned in training mode (see "training:")
    scale: skill
    fields:
      # Combat
      - { id: melee_combat,  label: Melee combat,  type: number, min: 0, max: 5, default: 0, icon: melee_combat.svg,  color: "#c68d80" }
      - { id: ranged_combat, label: Ranged combat, type: number, min: 0, max: 5, default: 0, icon: ranged_combat.svg, color: "#c68d80" }
      # Social
      - { id: manipulation,  label: Manipulation,  type: number, min: 0, max: 5, default: 0, icon: manipulation.svg,  color: "#ddac88" }
      - { id: performance,   label: Performance,   type: number, min: 0, max: 5, default: 0, icon: performance.svg,   color: "#ddac88" }
      # Physical
      - { id: acrobatics,    label: Acrobatics,    type: number, min: 0, max: 5, default: 0, icon: acrobatics.svg,    color: "#cfccca" }
      - { id: athletics,     label: Athletics,     type: number, min: 0, max: 5, default: 0, icon: athletics.svg,     color: "#cfccca" }
      - { id: stealth,       label: Stealth,       type: number, min: 0, max: 5, default: 0, icon: stealth.svg,       color: "#cfccca" }
      # Other
      - { id: medicine,      label: Medicine,      type: number, min: 0, max: 5, default: 0, icon: medicine.svg,      color: "#f2d08d" }
      - { id: theory,        label: Theory,        type: number, min: 0, max: 5, default: 0, icon: theory.svg,        color: "#f2d08d" }
      - { id: thieving,      label: Thieving,      type: number, min: 0, max: 5, default: 0, icon: thieving.svg,      color: "#f2d08d" }
      - { id: crafting,      label: Crafting,      type: number, min: 0, max: 5, default: 0, icon: crafting.svg,      color: "#f2d08d" }
      - { id: outdoorsman,   label: Outdoorsman,   type: number, min: 0, max: 5, default: 0, icon: outdoorsman.svg,   color: "#f2d08d" }

  - label: Gear & Notes
    fields:
      # "input: true": typed into a box (no − / +), for a number that jumps around freely.
      - { id: money, label: Money, type: number, min: 0, default: 0, input: true }
      # The equipment list: items the player (or the GM, for them) defines, each with modifiers
      # that apply while it is enabled. What an item may modify: abilities, skills, calculated
      # stats, and the item-only stats listed under "equipment:" below.
      - { id: equipment, label: Equipment, type: equipment }
      # Free text for everything that isn't worth a defined item.
      - { id: gear,  label: Gear,  type: text, lines: 4 }
      - { id: notes, label: Notes, type: text, lines: 3 }

# Boons and complications the GM rolls for (GM screen: "Roll boon" / "Roll complication").
# The GM picks a rank, the app rolls a die with one side per face listed here (5 faces → d5)
# and shows that face's entry at that rank. Every face needs the same number of ranks.
# The text is only read out — nothing is applied to a sheet automatically.
consequences:
  boons:
    - face: 1
      ranks:
        - { title: Benefit Inspired 1,          text: "+1 to your next roll until you rest or fail." }
        - { title: Permanent Benefit Hidden Help 2, text: "+1 to your roll when you cash it out" }
        - { title: Permanent Benefit Hidden Help 3, text: "+2 to your roll when you cash it out" }
    - face: 2
      ranks:
        - { title: Efficient 1, text: "Do it 33% faster (if applicable)" }
        - { title: Efficient 2, text: "Do it 50% faster (if applicable)" }
        - { title: Efficient 3, text: "Do it 75% faster (if applicable)" }
    - face: 3
      ranks:
        - { title: Lucky 1, text: "Find something useful (if applicable) in the value of up to 5" }
        - { title: Lucky 2, text: "Find something useful (if applicable) in the value of up to 10" }
        - { title: Lucky 3, text: "Find something useful (if applicable) in the value of up to 20" }
    - face: 4
      ranks:
        - { title: Improved confidence 1, text: "Regain 1 stamina or willpower" }
        - { title: Improved confidence 2, text: "Regain up to 2 stamina or willpower" }
        - { title: Improved confidence 3, text: "Restore fully either stamina or willpower" }
    - face: 5
      ranks:
        - { title: Benefit Inspired 1,    text: "+1 to your next roll until you rest or fail." }
        - { title: Improved situation 2, text: "This task becomes -1 easier for future checks" }
        - { title: Improved situation 3, text: "This task becomes -2 easier for future checks" }
  complications:
    - face: 1
      ranks:
        - { title: Potential harm 1, text: "Roll your endurance against easy injury" }
        - { title: Potential harm 2, text: "Roll your endurance against challenging injury" }
        - { title: Potential harm 3, text: "Roll your endurance against hard injury" }
    - face: 2
      ranks:
        - { title: Clumsy 1, text: "Do it 33% slower (if applicable)" }
        - { title: Clumsy 2, text: "Do it 50% slower (if applicable)" }
        - { title: Clumsy 3, text: "Do it 100% slower (if applicable)" }
    - face: 3
      ranks:
        - { title: Potential distress 1, text: "Roll your resolve against easy mental trauma" }
        - { title: Potential distress 2, text: "Roll your resolve against challenging mental trauma" }
        - { title: Potential distress 3, text: "Roll your resolve against hard mental trauma" }
    - face: 4
      ranks:
        - { title: Overworked,   text: "Lose 1 stamina or willpower" }
        - { title: Overworked 2, text: "Lose 2 stamina or willpower" }
        - { title: Overworked 3, text: "Lose completely willpower or stamina" }
    - face: 5
      ranks:
        - { title: Gain hindrance 1, text: "-1 to your next roll that GM will invoke" }
        - { title: Gain hindrance 2, text: "-2 to your next roll that GM will invoke" }
        - { title: Gain hindrance 3, text: "-3 to your next roll that GM will invoke" }

# Stats that exist only on items (shown with the item, used by no sheet row or formula).
equipment:
  item_stats:
    - { id: attack_damage,   label: Attack damage }
    - { id: attack_accuracy, label: Attack accuracy }

# Calculated values not placed in a section are listed here and shown in a compact block
# at the bottom of the sheet. All current ones live in the sections above.
derived: []

# Rolls intentionally left empty: the real rolling system (special dice, exertion,
# skill distribution, difficulty) hasn't been ported from the Godot project yet.
rolls: []
